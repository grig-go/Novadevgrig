
  # Nova (dev-Grig)

  This is a code bundle for Nova (dev-Grig). The original project is available at https://www.figma.com/design/xTeZQ36ELHcyp9M7s0yJsK/Nova--dev-Grig-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  