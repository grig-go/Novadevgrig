# File Server Configuration

# Root directory for file browsing (absolute path)
FILE_ROOT=/tmp/files

# Server port
PORT=8001

# Comma-separated list of allowed file extensions (without dots)
ALLOWED_EXTENSIONS=csv,tsv,txt,json,xml,xlsx,xls

# Maximum file size in bytes (default: 52428800 = 50MB)
MAX_FILE_SIZE=52428800
