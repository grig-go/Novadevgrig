
  # Nova App (supabase)

  This is a code bundle for Nova App (supabase). The original project is available at https://www.figma.com/design/2xOFDDJYesgXwWwdizemU1/Nova-App--supabase-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  